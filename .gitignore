node_modules/
dist/
.env
.env.local
.DS_Store
*.log
.vercel
.env*.local

# 대용량 임베딩 캐시 (Railway 빌드 시 자동 생성)
server/data/openai-embeddings-cache.json
server/data/embeddings-cache.json

# 프로젝트 로컬 백업 (실제 교사 대화 포함 — 커밋 금지)
backups/

# gstack 보안 감사 리포트 (로컬 전용)
.gstack/

# 링크 생성 파이프라인 실행 산출물 (진행상태/리포트)
scripts/mission_output/
scripts/mission_reports/

# Supabase CLI 임시 디렉터리 (프로젝트 ref 등 노출 방지)
supabase/.temp/
